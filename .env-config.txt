# LBX-Utils Path Configuration
# Copy this to .env.local for development or .env for production

# Path to the lbx-utils directory (relative or absolute)
# In development, this might be a relative path like '../lbx-utils'
# In production, use the absolute path to where you've installed lbx-utils
LBX_UTILS_PATH=../lbx-utils